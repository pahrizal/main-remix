import React from "react";
import GameResult from "~/components/GameResult";

const Test = () => {
    return <GameResult isTheWinner={true} id={""} name={""} socketId={""} bot={false} />;
};

export default Test;
